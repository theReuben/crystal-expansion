# Past the whole opening: new game, mum's speech, out of the front door.
# Leaves the player standing and unlocked in New Bark Town.
#
# D113: this used to be a hundred and fifty blind `press A` lines, tuned to how
# long the opening's dialogue took to read and retuned every time that changed.
# `newbark.txt` now answers mum's speech with `advance`, which waits on the
# script context rather than on a frame count, so all that is left here is one
# more `advance` for anything New Bark fires on the way out.
#
# progression.py and sweep.py both snapshot this state and teleport out of it
# with gPlaytestWarp, so where the player ends up matters less than that the
# player is unlocked and no script is running.
include newbark.txt
advance
player
